from django.apps import AppConfig
class DigitaltwinConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.digital_twin"
    label = "tap_dtwin"
    verbose_name = "Digital Twin Simulation"
