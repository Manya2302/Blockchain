from django.apps import AppConfig
class SelfhealingConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.self_healing"
    label = "tap_heal"
    verbose_name = "Self-Healing Blockchain Infrastructure"
