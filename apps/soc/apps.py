from django.apps import AppConfig
class SOCConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.soc'
    label = 'tap_soc'
    verbose_name = 'Security Operations Center'
