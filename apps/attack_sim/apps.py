from django.apps import AppConfig
class AttackSimConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.attack_sim'
    label = 'tap_attack_sim'
    verbose_name = 'Attack Simulation'
