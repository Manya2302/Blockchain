from django.apps import AppConfig
class GlobalintelConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.global_intel"
    label = "tap_gintel"
    verbose_name = "Global Intelligence & Threat Map"
